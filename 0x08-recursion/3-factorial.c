#include "main.h"

/**
 * factorial - finds factorial of number
 * @n: number
 * Return: factorial of number
 */
int factorial(int n)
{
	int fact;

	if (n == 0)
		return (-1);
	else if (n == 0)
		return (1)
	fact = n * factorial(n - 1);
	return (fact);
}
