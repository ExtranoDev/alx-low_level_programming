#include "main.h"

/**
 * _isdigit - detemines if input is a number
 * @c: input
 * Return: based in satisfied condition
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}

	return (0);
}
